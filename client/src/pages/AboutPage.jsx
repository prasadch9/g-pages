import React from 'react';
import { Link } from 'react-router-dom';

export default function AboutPage() {
  return <div className="container-page py-12 sm:py-20"><div className="grid gap-12 lg:grid-cols-[1fr_0.8fr] lg:items-center"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-vermilion">About Google Pages</p><h1 className="mt-3 font-display text-4xl font-semibold leading-tight text-ink sm:text-6xl">A clearer way to discover the places that make a city work.</h1><p className="mt-6 max-w-xl text-lg leading-relaxed text-ink/60">Google Pages is a location-first directory for trusted schools, hospitals, restaurants, businesses, and organizations. We connect people with useful local information and give owners a simple way to be found.</p><Link to="/explore" className="mt-8 inline-block rounded-lg bg-vermilion px-5 py-3 text-sm font-medium text-paper">Explore the directory</Link></div><div className="rounded-3xl bg-marigold/30 p-8"><div className="grid grid-cols-2 gap-3">{[['23+', 'categories'], ['6', 'cities seeded'], ['138', 'demo places'], ['24/7', 'discoverability']].map(([value, label]) => <div key={label} className="rounded-2xl bg-paper/80 p-5"><div className="font-display text-3xl font-semibold text-ink">{value}</div><div className="mt-1 text-sm text-ink/55">{label}</div></div>)}</div></div></div><div className="mt-20 grid gap-5 md:grid-cols-3">{[['Useful by default', 'Search by place, category, and city without getting lost in a generic results page.'], ['Built for trust', 'Verified listings, transparent reviews, and an approval workflow help keep information useful.'], ['Local first', 'Every result is anchored to a real location so discovery feels relevant, not random.']].map(([title, text]) => <div key={title} className="rounded-2xl border border-line bg-white/70 p-6"><h2 className="font-display text-xl font-semibold text-ink">{title}</h2><p className="mt-3 text-sm leading-relaxed text-ink/55">{text}</p></div>)}</div></div>;
}
